apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast-block
provisioner: csi.truenas.io
parameters:
  protocol: "nvmeof"
  pool: "ssd-pool"
  datasetPath: "k8s/control-plane/nvmeof"
  compression: "LZ4"
  sparse: "true"
reclaimPolicy: Delete
volumeBindingMode: Immediate
allowVolumeExpansion: true
