apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Cilium 1.20 uses the Gateway API v1.6.1 Standard channel. The existing
  # Kamaji TLSRoute is already gateway.networking.k8s.io/v1.
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.6.1/standard-install.yaml
